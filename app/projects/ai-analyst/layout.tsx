export const metadata = {
  title: 'Smart Paddle Sensing',
  description: 'Patent-pending smart paddle sensing concept for pickleball telemetry, coaching analytics, and broadcast data products.'
}

export default function AIAnalystLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
